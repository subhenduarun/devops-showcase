iname: CI/CD Pipeline for DevOps Showcase

on:
  push:
    branches:
      - master

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub (hardcoded for local testing only)
        uses: docker/login-action@v2
        with:
          username: subhendu083
          password: dckr_pat_1qSrYM99QEDU3VDgeRQWRc3Pl2Y

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: subhendu083/devops-showcase:latest
